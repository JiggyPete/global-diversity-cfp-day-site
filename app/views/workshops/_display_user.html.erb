<strong>
  <% if user.awaiting_invite_acceptance? %>
    PENDING
  <% end %>
  <%= user_type %>:
</strong>
<%= user_name user %>
<strong>CoC Training: </strong> <%= user.coc_training_complete? ? "✅" : "🚫" %>
<% if current_user.organiser? %>
  <%= link_to 'Delete', user_path(user), method: :delete, class: 'btn btn-warning', data: { confirm: 'Are you sure?' } %>
<% end %>
